# Fish completion for qeeg_version_cli

complete -c qeeg_version_cli -s h -l help -d "Show help"
complete -c qeeg_version_cli -l full -d "Print additional build details"
complete -c qeeg_version_cli -l json -d "Output JSON (useful for scripts)"
